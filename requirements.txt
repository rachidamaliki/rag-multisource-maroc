# ============================================================
# RAG FROM ZERO — dependances (100% gratuit, aucun service payant)
# Python 3.10 ou 3.11 recommande (3.12 ok, 3.13 a eviter pour l'instant)
# ============================================================

# --- Coeur scientifique -------------------------------------
numpy>=1.26
pandas>=2.1
scipy>=1.11
matplotlib>=3.8

# --- Tokenisation / decoupage -------------------------------
tiktoken>=0.7            # compter les tokens comme le fait un LLM
regex>=2024.5

# --- Embeddings & reranking (locaux, gratuits) --------------
sentence-transformers>=3.0
torch>=2.2               # CPU suffit ; GPU si dispo (Colab)
transformers>=4.44

# --- Recherche vectorielle ----------------------------------
faiss-cpu>=1.8           # index vectoriel local, gratuit
psycopg2-binary>=2.9     # pour pgvector (Arc 3, via Docker)
pgvector>=0.3

# --- Recherche lexicale -------------------------------------
rank-bm25>=0.2.2         # reference pour verifier VOTRE implementation

# --- Generation (API gratuites) -----------------------------
groq>=0.11               # Llama 3.3 70B, quota gratuit
google-genai>=0.3        # Gemini, quota gratuit (alternative)
httpx>=0.27
tenacity>=8.2            # retry / backoff maison

# --- Extraction de documents --------------------------------
pymupdf>=1.24            # PDF -> texte (rapide, gere l'arabe)
beautifulsoup4>=4.12
lxml>=5.2

# --- Visualisation / analyse --------------------------------
umap-learn>=0.5          # reduction 2D (Arc 0)
scikit-learn>=1.4        # UNIQUEMENT pour verifier vos implementations
seaborn>=0.13

# --- API & demo ---------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
streamlit>=1.36

# --- Utilitaires --------------------------------------------
python-dotenv>=1.0
tqdm>=4.66
rich>=13.7

# --- Tests (Arc 6 : regression testing) ---------------------
pytest>=8.2
